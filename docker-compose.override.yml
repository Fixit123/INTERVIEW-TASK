# Development overrides for docker-compose
# This file is automatically loaded by docker-compose

services:
  # Backend will use the Dockerfile CMD which includes migrations
  # No override needed - migrations run automatically
  backend: {}
